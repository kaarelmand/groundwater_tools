# Additions to PitFlow
* test different inflow terms
* add meltwater parameter
* make it possible to change flow units in PitFlow.report()
* carry area and precipitation changes over to PirFlowCommonUnits

# Combination helpers.
* Make a function that takes arbitrary length arrays of each variable and returns a
    product, containing PitFlow objects of all of them

* upload to github
* package